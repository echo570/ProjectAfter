services:
  - type: web
    name: stranger-chat
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npx tsx server/index-prod.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: ADMIN_PASSWORD
        generateValue: true
